---
description: "Recurring shapes and patterns that span multiple files"
---
# conventions

> **Generated by `fab docs-index`** — do not hand-edit; only the manual block below is preserved.

<!-- fab docs-index:manual:start -->
<!-- Hand-managed rows (non-markdown descriptions, external links, groupings) — preserved verbatim. -->
<!-- fab docs-index:manual:end -->

| File | Description |
|------|-------------|
| [docs-site-tree](docs-site-tree.md) | The `docs/site/**` documentation-tree consume/pull side (sibling of readme-extraction): the closed-set producer contract, site-absolute link resolution against the roster `mount`, the report-only closure + reserved-slug lints, the repo-tarball pull in `refresh-readme.yml`, the `[mount]/[...path].astro` dynamic route (`content/<slug>/site/<path>.md` → `/<mount>/<path>`), the first-H1 strip, roster-driven sidebar + legacy-mount redirect generation, and the `.ts`/`.mjs` config-eval split |
| [help-collection](help-collection.md) | The `help/<slug>.json` consume/pull side: the Zod anchor (`schemas.ts`), the repo-root `help/` collector, the rendering consumers (CommandReference/CommandIndex, homepage terminal cards, the llms endpoints, versions.json), the scheduled brew + `help-dump` pull (`refresh-help.yml` — the `hexokit:run-kit:run-kit` triple; the site slug may differ from repo/formula/binary per the four-name rule in tool-roster), and the two-mode command/flag cross-check. Contract: `docs/specs/help-dump-contract.md` |
| [readme-extraction](readme-extraction.md) | The README consume/pull side (sibling of help-collection): the deduction rules in `extract-readme.ts`, the repo-root `content/<slug>/` collector, `ReadmeSlice.astro` on the per-tool `readme` page, the `readme-toc.ts` rail builder, the `/llms-full.txt` dump, the `refresh-readme.yml` always-commit daily pull (the `hexokit:run-kit` slug:repo pair, both steps in lockstep), and the report-only divergence reporter + the CLI's four exit behaviors. Contract: `docs/specs/readme-extraction-contract.md` |
| [seo-social-meta](seo-social-meta.md) | The SEO & social-share layer plus its agent-discoverability and editorial siblings: Starlight's baseline, the site-wide og:image set in `Head.astro`, the 1200×630 `og-image.png` (wordmark `hexokit`) + generator, homepage `head:` overrides, the JSON-LD route dispatcher (homepage graph naming HexoKit; mount-gated per-tool graph with Docs/Toolkit breadcrumbs), `/llms.txt` + `/llms-full.txt` (roster-driven), and the editorial layer (hand-authored meta descriptions, cross-tool link graph) |
| [tool-page-rubric](tool-page-rubric.md) | The shape of every tool page on the live site: the site-authored overview body (GithubButton + job-framed lead + Install/Screenshots/How-it-fits sections; the command/flag hard rule binds the framing prose), the generated `commands`/`readme` sibling pages, site chrome overrides (hexagon logo/favicon, HeaderNav SocialIcons slot, Footer/Head, ToC dispatchers), per-tool pages canonical at the roster mount (product overview at `/docs/`), `/tools/` retired into `/toolkit/`, root-namespace reservation |
| [tool-roster](tool-roster.md) | The single site-authored tool roster (`src/lib/tool-roster.mjs`, plain ESM for config-eval; `tool-slugs.ts` is its typed re-export): one record per tool with the four names — slug (pulled-data key), mount (URL segment), repo (GitHub), formula/binary — plus `legacyMounts` and the `mountFor`/`slugForMount`/`repoFor`/`labelFor`/`isToolSlug`/`isToolMount` helpers. HexoKit is the first slug≠mount≠repo tool (`hexokit`/`docs`/`run-kit`); no consumer hardcodes `/<slug>/` or `sahil87/<slug>`. |
| [versions-manifest](versions-manifest.md) | The `/versions.json` version manifest — logic in `src/lib/versions-manifest.ts`. The repo-root `versions-policy.json` is the roster, keyed by the consumer-facing roster name (matched by `shll check-updates`); `latest` is the envelope `version` stripped to bare, the optional `envelope` naming the help file when it differs from the key (the `run-kit` row reads `help/hexokit.json`). Pulled envelopes skip-degrade; the authored policy build-stops. Contract: `docs/specs/versions-manifest-contract.md` |
